"use client";

/**
 * Optional overlay on top of the site background image.
 * The actual background image is set on body in globals.css (BACKGROUND.PNG).
 */
export function HQBackground() {
  return null;
}
